import { registerFormSchemaType } from '../pages/Register';

export const registerUser = (userData: registerFormSchemaType) => {
  console.log('this will be an axios request to our backend. Destructure the data and return it')
};
